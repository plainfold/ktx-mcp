# Server-side only (hosted). End users never need this key.
DATA_GO_KR_SERVICE_KEY=

# Hosted cache (required for multi-user)
REDIS_URL=redis://localhost:6379/0

# Optional
KTX_MCP_TRANSPORT=stdio
KTX_MCP_DEFAULT_LOCALE=en
KTX_MCP_CACHE_TTL=900
