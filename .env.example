# --- Fly + Supabase (hosted) ---
DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres
SYNC_SECRET=generate-a-long-random-string

# Server-side only (TAGO sync on Fly — never expose to MCP clients)
DATA_GO_KR_SERVICE_KEY=

# Fly / HTTP
KTX_MCP_TRANSPORT=http
KTX_MCP_PORT=8080
KTX_MCP_DEFAULT_LOCALE=en
