# Copy to `.env` in the repo root (never commit `.env`).

# TAGO TrainInfo API — Decoding key from data.go.kr 15098552
DATA_GO_KR_SERVICE_KEY=

# Supabase pooler (session :5432 local dev, transaction :6543 Fly reads)
DATABASE_URL=

# Protects POST /internal/sync (set same value in pg_cron caller)
SYNC_SECRET=

# stdio for Cursor local BYOK; http for Fly.io
KTX_MCP_TRANSPORT=stdio
KTX_MCP_PORT=8080
KTX_MCP_DEFAULT_LOCALE=en
